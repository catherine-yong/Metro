    //bout de code qui peuvent etre utiles : 
    /*FILE* fichier;
    fichier = fopen ("ligne_metro.csv", "rt");
       
    if(fichier == NULL)
    {
        printf("ouverture impossible du fichier \n");
        return 0;
    }
    
    char ligne[100];
    
    Une_ligne* New= malloc(sizeof(Une_ligne));
    New->suiv = NULL;  // Le dernier élément pointe sur rien.
 
  // Allocation mémoire pour les valeurs
    New-> code = (char*)malloc(sizeof(char)*strlen(a));
    New-> color = (char*)malloc(sizeof(char)*strlen(b));
    New-> intervalle = *(float*)malloc(sizeof(d));
    New-> vitesse = *(float*)malloc(sizeof(d));
    Une_ligne* Tmp = liste;


    memcpy(New->code, a, sizeof(char)*strlen(a));
    memcpy(New->color, b, sizeof(char)*strlen(b));*/
    
     /* while (fgets(ligne,100,fichier) != NULL)
        {
          a = strtok(ligne,";");
          b = strtok(NULL,";");
          c = strtok(ligne,";");
          d = strtok(NULL,";");
          liste=add_element(liste,a,b,c,d);
        }*/


